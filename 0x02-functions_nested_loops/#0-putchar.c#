#include "main.h"

/**
*main - Entry point
*Return: Always 0 (Success)
*/
int main(void)
{
  char ch = '_';
putchar(ch);
char ch = 'p';
putchar(ch);
 char ch = 'u';
putchar(ch);
 char ch = 't';
putchar(ch);
 char ch = 'c';
putchar(ch);
 char ch = 'h';
putchar(ch);
 char ch = 'a';
 putchar(ch);
 char ch = 'r';
putchar(ch);
 char ch = '\n';
putchar(ch);
  
return (0);
}
